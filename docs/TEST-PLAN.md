# Test Plan

Task 001 must include automated checks for the health route, permission helpers, unsuccessful authentication, protected-route access, and administrator access.

Every task must pass linting, type checks, unit tests, relevant integration tests, browser tests, database migration checks, and a production build.

Visual review targets are 390 px mobile, 768 px tablet, and 1440 px desktop. Verify keyboard navigation, visible focus, loading, empty, validation, error, and success states, with no horizontal overflow.
